<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Cuestionario</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
      .hidden {
        display: none;
      }
    </style>
  </head>
  <body class="bg-gray-100 p-8">
    <div class=" mx-auto p-4 bg-white rounded-lg shadow-md">
      <h1 class="text-2xl font-bold mb-4 text-indigo-600 text-center">Cuestionario</h1>
      <form id="quizForm" class="space-y-4">
        <!-- Matemáticas -->
        <div class="space-y-2">
          <h2 class="text-xl font-semibold text-gray-800">Matemáticas</h2>
          <div class="flex flex-col md:flex-row md:space-x-4">
            <div class="flex-grow mb-4">
              <label class="block text-gray-700">1. ¿Cuánto es 5 x 3?</label>
              <input type="number" class="w-full p-2 border rounded" name="math1" />
            </div>
            <div class="flex-grow mb-4">
              <label class="block text-gray-700">2. ¿Cuánto es 12 ÷ 4?</label>
              <input type="number" class="w-full p-2 border rounded" name="math2" />
            </div>
          </div>
        </div>

        <!-- Ciencias Sociales -->
        <div class="space-y-2">
          <h2 class="text-xl font-semibold text-gray-800">Ciencias Sociales</h2>
          <div class="flex flex-col md:flex-row md:space-x-4">
            <div class="flex-grow mb-4">
              <label class="block text-gray-700">1. ¿Cuál es el propósito de los derechos humanos en una sociedad?</label>
              <input type="text" class="w-full p-2 border rounded" name="sociales1" />
            </div>
            <div class="flex-grow mb-4">
              <label class="block text-gray-700">2. ¿Qué es la anarquía?</label>
              <input type="text" class="w-full p-2 border rounded" name="sociales2" />
            </div>
          </div>
        </div>

        <!-- Ciencias Naturales -->
        <div class="space-y-2">
          <h2 class="text-xl font-semibold text-gray-800">Ciencias Naturales</h2>
          <div class="flex flex-col md:flex-row md:space-x-4">
            <div class="flex-grow mb-4">
              <label class="block text-gray-700">1. ¿Qué gas necesitan las plantas para realizar la fotosíntesis?</label>
              <input type="text" class="w-full p-2 border rounded" name="naturales1" />
            </div>
            <div class="flex-grow mb-4">
              <label class="block text-gray-700">2. ¿Qué es la clorofila?</label>
              <input type="text" class="w-full p-2 border rounded" name="naturales2" />
            </div>
          </div>
        </div>

        <!-- Ética -->
        <div class="space-y-2">
          <h2 class="text-xl font-semibold text-gray-800">Ética</h2>
          <div class="flex flex-col md:flex-row md:space-x-4">
            <div class="flex-grow mb-4">
              <label class="block text-gray-700">1. ¿Por qué es importante el valor de la responsabilidad en la toma de decisiones?</label>
              <input type="text" class="w-full p-2 border rounded" name="etica1" />
            </div>
            <div class="flex-grow mb-4">
              <label class="block text-gray-700">2. ¿Cómo se manifiesta el valor del respeto en la toma de decisiones éticas?</label>
              <input type="text" class="w-full p-2 border rounded" name="etica2" />
            </div>
          </div>
        </div>

        <!-- Lengua Castellana -->
        <div class="space-y-2">
          <h2 class="text-xl font-semibold text-gray-800">Lengua Castellana</h2>
          <div class="flex flex-col md:flex-row md:space-x-4">
            <div class="flex-grow mb-4">
              <label class="block text-gray-700">1. ¿Qué es una metáfora en la escritura?</label>
              <input type="text" class="w-full p-2 border rounded" name="lengua1" />
            </div>
            <div class="flex-grow mb-4">
              <label class="block text-gray-700">2. ¿Qué es la ironía en un texto literario?</label>
              <input type="text" class="w-full p-2 border rounded" name="lengua2" />
            </div>
          </div>
        </div>

        <!-- Nombres de Animales -->
        <div class="space-y-2">
          <h2 class="text-xl font-semibold text-gray-800">Nombres de Animales</h2>
          <div class="flex flex-col md:flex-row md:space-x-4">
            <div class="flex-grow mb-4">
              <label class="block text-gray-700">1. ¿Cómo se llama el mamífero más grande del mundo?</label>
              <input type="text" class="w-full p-2 border rounded" name="animal1" />
            </div>
            <div class="flex-grow mb-4">
              <label class="block text-gray-700">2. ¿Cuál es el animal más rápido del mundo?</label>
              <input type="text" class="w-full p-2 border rounded" name="animal2" />
            </div>
          </div>
        </div>

        <!-- Cultura General -->
        <div class="space-y-2">
          <h2 class="text-xl font-semibold text-gray-800">Cultura General</h2>
          <div class="flex flex-col md:flex-row md:space-x-4">
            <div class="flex-grow mb-4">
              <label class="block text-gray-700">1. ¿Cuál es la capital de Japón?</label>
              <input type="text" class="w-full p-2 border rounded" name="culture1" />
            </div>
            <div class="flex-grow mb-4">
              <label class="block text-gray-700">2. ¿Quién pintó la Mona Lisa?</label>
              <input type="text" class="w-full p-2 border rounded" name="culture2" />
            </div>
          </div>
        </div>

        <button type="button" onclick="submitQuiz()" class="mt-4 bg-indigo-600 text-white px-4 py-2 rounded hover:bg-indigo-700 mx-auto">Enviar</button>
      </form>

      <div id="results" class="hidden mt-4 p-4 bg-green-100 rounded">
        <h2 class="text-xl font-semibold mb-2">Resultados</h2>
        <p id="resultsText" class="text-gray-700"></p>
      </div>
    </div>
    <div class="w-full flex justify-center mt-4">
      <a href="http://localhost:4321/" class="inline-block p-1 text-lg font-bold bg-white text-center border-2 border-black rounded-lg hover:bg-yellow-300">
          Volver al inicio
      </a>
  </div>

    <script>
      function submitQuiz() {
        const form = document.getElementById('quizForm');
        const formData = new FormData(form);
        const results = document.getElementById('results');
        const resultsText = document.getElementById('resultsText');

        const answers = {
          math1: '15',
          math2: '3',
          sociales1: 'Proteger los derechos básicos y la dignidad de todas las personas.',
          sociales2: 'Ausencia de un gobierno central.',
          naturales1: 'Dióxido de carbono',
          naturales2: 'Pigmento verde en las hojas de las plantas.',
          etica1: 'Implica asumir las consecuencias de las propias acciones.',
          etica2: 'Considerando las perspectivas y derechos de los demás.',
          lengua1: 'Una comparación directa entre dos elementos.',
          lengua2: 'Una contradicción entre lo que se dice y lo que se quiere expresar.',
          animal1: 'La ballena azul.',
          animal2: 'El guepardo.',
          culture1: 'Tokio',
          culture2: 'Leonardo da Vinci'
        };

        let correctCount = 0;
        let totalQuestions = 14;

        for (let [key, value] of formData.entries()) {
          if (value.toString().toLowerCase() === answers[key].toLowerCase()) {
            correctCount++;
          }
        }

        let score = (correctCount / totalQuestions) * 5;
        score = score.toFixed(2);

        let output = `Respuestas correctas: ${correctCount} de ${totalQuestions}\n`;
        output += `Tu nota es: ${score} de 5.0`;

        resultsText.textContent = output;
        results.classList.remove('hidden');
      }
    </script>
  </body>
</html>
